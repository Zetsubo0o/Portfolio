/**
 * ─── SMILE THEME KILL SWITCH ───
 *
 * Set SMILE to false to revert the entire SMILE experiment in one edit:
 * palette returns to Midnight Amber (dark default + light toggle),
 * ticker copy, chat icon, hero smile-arc, emoji and CTA copy all restore.
 *
 * Optional deeper cleanup after flipping to false:
 * - delete the `html.smile` blocks in src/index.css
 * - delete this file and its imports (App, Ticker, ChatWidget, Home)
 */
export const SMILE = true
